# AudioSwitch

> 一款轻量、美观的 Windows 音频设备切换工具

---

## 简介

AudioSwitch 是一款专为 Windows 用户设计的音频设备快速切换工具。它解决了 Windows 系统中**麦克风切换操作繁琐**的痛点——原本需要层层点击"设置"才能完成的操作，现在**一键或一个快捷键**就能搞定。

**一句话介绍**：让切换麦克风和音箱，像点一下鼠标那么简单。

---

## 为什么需要 AudioSwitch？

Windows 11 的音频管理有一个"不对称"的设计：
- **音频输出（播放设备）**：已经很好用，右下角图标一点就能切换。
- **麦克风（输入设备）**：切换非常麻烦，必须钻进"设置"里一层层点。

对于同时拥有多套麦克风设备的用户（耳机麦、USB 麦、蓝牙麦、虚拟麦等），频繁切换麦克风是一件很痛苦的事。AudioSwitch 就是为了解决这个问题而生的。

---

## 核心功能

### 🎤 一键切换麦克风
- 自动列出电脑上所有输入设备（2.4G 耳机麦、USB 麦、蓝牙麦、虚拟麦等）
- 点击即可切换当前默认麦克风
- 支持**全局快捷键**切换（默认 Ctrl+Alt+F9），无需打开窗口

### 🔊 一键切换音频输出
- 自动列出所有输出设备（音箱、耳机、蓝牙耳机等）
- 点击即可切换当前默认输出设备
- 支持**全局快捷键**切换（默认 Ctrl+Alt+F10）

### 🖥️ 系统托盘常驻
- 最小化后收在任务栏右下角托盘，不占屏幕
- 右键托盘图标即可看到设备列表，直接切换
- 托盘菜单显示当前默认设备名，一目了然

### 🎯 智能设备过滤
- **智能模式**：显示全部真实设备（含板载声卡），仅排除虚拟声卡、显卡音频输出、混音回环等杂项
- **全部模式**：显示所有设备
- **USB/蓝牙模式**：只显示外部连接的设备

### ⚡ 开机自启动
- 提供开关，用户可自由选择是否开机自动启动
- 默认关闭，由用户手动打开

### ⌨️ 自定义快捷键
- 支持自定义麦克风和音箱的切换快捷键
- 支持 Ctrl、Alt、Shift、Win 等组合键

### 🌙 深色模式
- 支持浅色/深色/跟随系统三种主题模式
- 深色模式下文字对比度优化，清晰易读

### 💾 窗口位置记忆
- 记住窗口的位置和大小
- 下次打开时自动恢复到上次的位置

---

## 界面特色

- **Win11 风格设计**：圆角卡片、渐变背景、柔和阴影
- **自定义标题栏**：无边框窗口 + 自定义最小化/最大化/关闭按钮
- **流畅动画**：按钮 hover 放大、按下回弹、卡片浮起效果
- **现代滑动开关**：Win11 风格的开关控件，替代传统复选框
- **精致滚动条**：细条圆角滚动条，hover 变色
- **设置抽屉**：右侧滑入的设置面板，带半透明遮罩

---

## 技术架构

### 技术栈
- **语言**：C# + WPF + .NET 8.0
- **音频 API**：NAudio 2.2.1
- **设备切换**：COM 接口 IPolicyConfig
- **托盘管理**：Windows Forms NotifyIcon

### 项目结构
```
AudioSwitch/
├── Core/
│   └── DeviceSwitcher.cs      # 设备切换核心（COM 调用）
├── Models/
│   └── AudioDevice.cs         # 设备数据模型
├── Services/
│   ├── DeviceService.cs       # 设备枚举、过滤、缓存
│   ├── ThemeService.cs        # 主题切换（浅色/深色）
│   ├── TrayService.cs         # 托盘图标、菜单管理
│   ├── HotkeyManager.cs       # 快捷键管理
│   ├── HotkeyService.cs       # Win32 热键 API
│   ├── SettingsPanelManager.cs # 设置面板管理
│   ├── SettingsManager.cs     # 设置读写（JSON）
│   └── AutoStartService.cs    # 开机自启动
├── MainWindow.xaml / .cs      # 主窗口（组装逻辑）
├── ClosePromptWindow.xaml     # 关闭确认对话框
└── App.xaml / .cs             # 单实例启动
```

### 设计原则
- **模块化**：每个 Service 负责一个独立功能，互不干扰
- **按需刷新**：设备列表只在用户操作时刷新，避免无谓的 UI 闪烁
- **后台加载**：设备枚举在后台线程执行，不阻塞 UI
- **设备缓存**：设备元数据（名称、总线类型）缓存复用，减少 COM 调用

---

## 使用方法

### 基本操作
1. 双击 `AudioSwitch.exe` 启动（免安装）
2. 主窗口显示麦克风列表和音箱列表
3. 点击设备即可切换默认设备
4. 点击 X 按钮最小化到托盘，继续后台运行

### 快捷键
- **Ctrl + Alt + F9**：切换到下一个麦克风
- **Ctrl + Alt + F10**：切换到下一个音箱
- 快捷键可在设置中自定义

### 托盘菜单
- 右键托盘图标，显示设备列表
- 当前默认设备前有 ✓ 标记
- 标题栏显示当前默认设备名

### 设置选项
- **关闭行为**：询问/最小化到托盘/直接退出
- **主题模式**：浅色/深色/跟随系统
- **设备显示范围**：智能/全部/仅 USB 和蓝牙
- **快捷键设置**：自定义切换快捷键
- **开机自启动**：开关

---

## 系统要求

- **操作系统**：Windows 10 / Windows 11
- **运行时**：.NET 8.0 Desktop Runtime（安装版已内置，便携版需自行安装）
- **权限**：普通用户权限即可

---

## 下载

### 便携版（推荐）
- 单个 exe 文件，双击直接运行
- 需要系统已安装 .NET 8.0 Desktop Runtime
- 文件大小：约 0.9 MB

### 安装版
- 包含 .NET 运行时，无需额外安装
- 支持覆盖安装（保留设置）
- 文件大小：约 57MB

---

## 开发历程

### v1.0.0（初始版本）
- 核心功能：麦克风/音箱切换、托盘菜单、快捷键
- 基础 UI：Win11 风格界面

### v1.0.1（当前版本）
- **性能优化**：设备信息缓存、按需刷新、设置保存不再卡顿
- **架构重构**：代码模块化（8 个独立 Service）
- **UI 升级**：
  - 自定义标题栏（无边框 + 自定义按钮）
  - Win11 风格滑动开关
  - 卡片 hover 浮起效果
  - 渐变背景、按钮动画
  - 现代滚动条
  - 深色模式优化
- **功能增强**：窗口位置记忆、托盘菜单显示当前设备名
- **稳定性**：错误处理加固、单实例启动

---

## 许可证

[MIT](LICENSE)

---

## 联系方式

如有问题或建议，欢迎在 GitHub 提 Issue。
