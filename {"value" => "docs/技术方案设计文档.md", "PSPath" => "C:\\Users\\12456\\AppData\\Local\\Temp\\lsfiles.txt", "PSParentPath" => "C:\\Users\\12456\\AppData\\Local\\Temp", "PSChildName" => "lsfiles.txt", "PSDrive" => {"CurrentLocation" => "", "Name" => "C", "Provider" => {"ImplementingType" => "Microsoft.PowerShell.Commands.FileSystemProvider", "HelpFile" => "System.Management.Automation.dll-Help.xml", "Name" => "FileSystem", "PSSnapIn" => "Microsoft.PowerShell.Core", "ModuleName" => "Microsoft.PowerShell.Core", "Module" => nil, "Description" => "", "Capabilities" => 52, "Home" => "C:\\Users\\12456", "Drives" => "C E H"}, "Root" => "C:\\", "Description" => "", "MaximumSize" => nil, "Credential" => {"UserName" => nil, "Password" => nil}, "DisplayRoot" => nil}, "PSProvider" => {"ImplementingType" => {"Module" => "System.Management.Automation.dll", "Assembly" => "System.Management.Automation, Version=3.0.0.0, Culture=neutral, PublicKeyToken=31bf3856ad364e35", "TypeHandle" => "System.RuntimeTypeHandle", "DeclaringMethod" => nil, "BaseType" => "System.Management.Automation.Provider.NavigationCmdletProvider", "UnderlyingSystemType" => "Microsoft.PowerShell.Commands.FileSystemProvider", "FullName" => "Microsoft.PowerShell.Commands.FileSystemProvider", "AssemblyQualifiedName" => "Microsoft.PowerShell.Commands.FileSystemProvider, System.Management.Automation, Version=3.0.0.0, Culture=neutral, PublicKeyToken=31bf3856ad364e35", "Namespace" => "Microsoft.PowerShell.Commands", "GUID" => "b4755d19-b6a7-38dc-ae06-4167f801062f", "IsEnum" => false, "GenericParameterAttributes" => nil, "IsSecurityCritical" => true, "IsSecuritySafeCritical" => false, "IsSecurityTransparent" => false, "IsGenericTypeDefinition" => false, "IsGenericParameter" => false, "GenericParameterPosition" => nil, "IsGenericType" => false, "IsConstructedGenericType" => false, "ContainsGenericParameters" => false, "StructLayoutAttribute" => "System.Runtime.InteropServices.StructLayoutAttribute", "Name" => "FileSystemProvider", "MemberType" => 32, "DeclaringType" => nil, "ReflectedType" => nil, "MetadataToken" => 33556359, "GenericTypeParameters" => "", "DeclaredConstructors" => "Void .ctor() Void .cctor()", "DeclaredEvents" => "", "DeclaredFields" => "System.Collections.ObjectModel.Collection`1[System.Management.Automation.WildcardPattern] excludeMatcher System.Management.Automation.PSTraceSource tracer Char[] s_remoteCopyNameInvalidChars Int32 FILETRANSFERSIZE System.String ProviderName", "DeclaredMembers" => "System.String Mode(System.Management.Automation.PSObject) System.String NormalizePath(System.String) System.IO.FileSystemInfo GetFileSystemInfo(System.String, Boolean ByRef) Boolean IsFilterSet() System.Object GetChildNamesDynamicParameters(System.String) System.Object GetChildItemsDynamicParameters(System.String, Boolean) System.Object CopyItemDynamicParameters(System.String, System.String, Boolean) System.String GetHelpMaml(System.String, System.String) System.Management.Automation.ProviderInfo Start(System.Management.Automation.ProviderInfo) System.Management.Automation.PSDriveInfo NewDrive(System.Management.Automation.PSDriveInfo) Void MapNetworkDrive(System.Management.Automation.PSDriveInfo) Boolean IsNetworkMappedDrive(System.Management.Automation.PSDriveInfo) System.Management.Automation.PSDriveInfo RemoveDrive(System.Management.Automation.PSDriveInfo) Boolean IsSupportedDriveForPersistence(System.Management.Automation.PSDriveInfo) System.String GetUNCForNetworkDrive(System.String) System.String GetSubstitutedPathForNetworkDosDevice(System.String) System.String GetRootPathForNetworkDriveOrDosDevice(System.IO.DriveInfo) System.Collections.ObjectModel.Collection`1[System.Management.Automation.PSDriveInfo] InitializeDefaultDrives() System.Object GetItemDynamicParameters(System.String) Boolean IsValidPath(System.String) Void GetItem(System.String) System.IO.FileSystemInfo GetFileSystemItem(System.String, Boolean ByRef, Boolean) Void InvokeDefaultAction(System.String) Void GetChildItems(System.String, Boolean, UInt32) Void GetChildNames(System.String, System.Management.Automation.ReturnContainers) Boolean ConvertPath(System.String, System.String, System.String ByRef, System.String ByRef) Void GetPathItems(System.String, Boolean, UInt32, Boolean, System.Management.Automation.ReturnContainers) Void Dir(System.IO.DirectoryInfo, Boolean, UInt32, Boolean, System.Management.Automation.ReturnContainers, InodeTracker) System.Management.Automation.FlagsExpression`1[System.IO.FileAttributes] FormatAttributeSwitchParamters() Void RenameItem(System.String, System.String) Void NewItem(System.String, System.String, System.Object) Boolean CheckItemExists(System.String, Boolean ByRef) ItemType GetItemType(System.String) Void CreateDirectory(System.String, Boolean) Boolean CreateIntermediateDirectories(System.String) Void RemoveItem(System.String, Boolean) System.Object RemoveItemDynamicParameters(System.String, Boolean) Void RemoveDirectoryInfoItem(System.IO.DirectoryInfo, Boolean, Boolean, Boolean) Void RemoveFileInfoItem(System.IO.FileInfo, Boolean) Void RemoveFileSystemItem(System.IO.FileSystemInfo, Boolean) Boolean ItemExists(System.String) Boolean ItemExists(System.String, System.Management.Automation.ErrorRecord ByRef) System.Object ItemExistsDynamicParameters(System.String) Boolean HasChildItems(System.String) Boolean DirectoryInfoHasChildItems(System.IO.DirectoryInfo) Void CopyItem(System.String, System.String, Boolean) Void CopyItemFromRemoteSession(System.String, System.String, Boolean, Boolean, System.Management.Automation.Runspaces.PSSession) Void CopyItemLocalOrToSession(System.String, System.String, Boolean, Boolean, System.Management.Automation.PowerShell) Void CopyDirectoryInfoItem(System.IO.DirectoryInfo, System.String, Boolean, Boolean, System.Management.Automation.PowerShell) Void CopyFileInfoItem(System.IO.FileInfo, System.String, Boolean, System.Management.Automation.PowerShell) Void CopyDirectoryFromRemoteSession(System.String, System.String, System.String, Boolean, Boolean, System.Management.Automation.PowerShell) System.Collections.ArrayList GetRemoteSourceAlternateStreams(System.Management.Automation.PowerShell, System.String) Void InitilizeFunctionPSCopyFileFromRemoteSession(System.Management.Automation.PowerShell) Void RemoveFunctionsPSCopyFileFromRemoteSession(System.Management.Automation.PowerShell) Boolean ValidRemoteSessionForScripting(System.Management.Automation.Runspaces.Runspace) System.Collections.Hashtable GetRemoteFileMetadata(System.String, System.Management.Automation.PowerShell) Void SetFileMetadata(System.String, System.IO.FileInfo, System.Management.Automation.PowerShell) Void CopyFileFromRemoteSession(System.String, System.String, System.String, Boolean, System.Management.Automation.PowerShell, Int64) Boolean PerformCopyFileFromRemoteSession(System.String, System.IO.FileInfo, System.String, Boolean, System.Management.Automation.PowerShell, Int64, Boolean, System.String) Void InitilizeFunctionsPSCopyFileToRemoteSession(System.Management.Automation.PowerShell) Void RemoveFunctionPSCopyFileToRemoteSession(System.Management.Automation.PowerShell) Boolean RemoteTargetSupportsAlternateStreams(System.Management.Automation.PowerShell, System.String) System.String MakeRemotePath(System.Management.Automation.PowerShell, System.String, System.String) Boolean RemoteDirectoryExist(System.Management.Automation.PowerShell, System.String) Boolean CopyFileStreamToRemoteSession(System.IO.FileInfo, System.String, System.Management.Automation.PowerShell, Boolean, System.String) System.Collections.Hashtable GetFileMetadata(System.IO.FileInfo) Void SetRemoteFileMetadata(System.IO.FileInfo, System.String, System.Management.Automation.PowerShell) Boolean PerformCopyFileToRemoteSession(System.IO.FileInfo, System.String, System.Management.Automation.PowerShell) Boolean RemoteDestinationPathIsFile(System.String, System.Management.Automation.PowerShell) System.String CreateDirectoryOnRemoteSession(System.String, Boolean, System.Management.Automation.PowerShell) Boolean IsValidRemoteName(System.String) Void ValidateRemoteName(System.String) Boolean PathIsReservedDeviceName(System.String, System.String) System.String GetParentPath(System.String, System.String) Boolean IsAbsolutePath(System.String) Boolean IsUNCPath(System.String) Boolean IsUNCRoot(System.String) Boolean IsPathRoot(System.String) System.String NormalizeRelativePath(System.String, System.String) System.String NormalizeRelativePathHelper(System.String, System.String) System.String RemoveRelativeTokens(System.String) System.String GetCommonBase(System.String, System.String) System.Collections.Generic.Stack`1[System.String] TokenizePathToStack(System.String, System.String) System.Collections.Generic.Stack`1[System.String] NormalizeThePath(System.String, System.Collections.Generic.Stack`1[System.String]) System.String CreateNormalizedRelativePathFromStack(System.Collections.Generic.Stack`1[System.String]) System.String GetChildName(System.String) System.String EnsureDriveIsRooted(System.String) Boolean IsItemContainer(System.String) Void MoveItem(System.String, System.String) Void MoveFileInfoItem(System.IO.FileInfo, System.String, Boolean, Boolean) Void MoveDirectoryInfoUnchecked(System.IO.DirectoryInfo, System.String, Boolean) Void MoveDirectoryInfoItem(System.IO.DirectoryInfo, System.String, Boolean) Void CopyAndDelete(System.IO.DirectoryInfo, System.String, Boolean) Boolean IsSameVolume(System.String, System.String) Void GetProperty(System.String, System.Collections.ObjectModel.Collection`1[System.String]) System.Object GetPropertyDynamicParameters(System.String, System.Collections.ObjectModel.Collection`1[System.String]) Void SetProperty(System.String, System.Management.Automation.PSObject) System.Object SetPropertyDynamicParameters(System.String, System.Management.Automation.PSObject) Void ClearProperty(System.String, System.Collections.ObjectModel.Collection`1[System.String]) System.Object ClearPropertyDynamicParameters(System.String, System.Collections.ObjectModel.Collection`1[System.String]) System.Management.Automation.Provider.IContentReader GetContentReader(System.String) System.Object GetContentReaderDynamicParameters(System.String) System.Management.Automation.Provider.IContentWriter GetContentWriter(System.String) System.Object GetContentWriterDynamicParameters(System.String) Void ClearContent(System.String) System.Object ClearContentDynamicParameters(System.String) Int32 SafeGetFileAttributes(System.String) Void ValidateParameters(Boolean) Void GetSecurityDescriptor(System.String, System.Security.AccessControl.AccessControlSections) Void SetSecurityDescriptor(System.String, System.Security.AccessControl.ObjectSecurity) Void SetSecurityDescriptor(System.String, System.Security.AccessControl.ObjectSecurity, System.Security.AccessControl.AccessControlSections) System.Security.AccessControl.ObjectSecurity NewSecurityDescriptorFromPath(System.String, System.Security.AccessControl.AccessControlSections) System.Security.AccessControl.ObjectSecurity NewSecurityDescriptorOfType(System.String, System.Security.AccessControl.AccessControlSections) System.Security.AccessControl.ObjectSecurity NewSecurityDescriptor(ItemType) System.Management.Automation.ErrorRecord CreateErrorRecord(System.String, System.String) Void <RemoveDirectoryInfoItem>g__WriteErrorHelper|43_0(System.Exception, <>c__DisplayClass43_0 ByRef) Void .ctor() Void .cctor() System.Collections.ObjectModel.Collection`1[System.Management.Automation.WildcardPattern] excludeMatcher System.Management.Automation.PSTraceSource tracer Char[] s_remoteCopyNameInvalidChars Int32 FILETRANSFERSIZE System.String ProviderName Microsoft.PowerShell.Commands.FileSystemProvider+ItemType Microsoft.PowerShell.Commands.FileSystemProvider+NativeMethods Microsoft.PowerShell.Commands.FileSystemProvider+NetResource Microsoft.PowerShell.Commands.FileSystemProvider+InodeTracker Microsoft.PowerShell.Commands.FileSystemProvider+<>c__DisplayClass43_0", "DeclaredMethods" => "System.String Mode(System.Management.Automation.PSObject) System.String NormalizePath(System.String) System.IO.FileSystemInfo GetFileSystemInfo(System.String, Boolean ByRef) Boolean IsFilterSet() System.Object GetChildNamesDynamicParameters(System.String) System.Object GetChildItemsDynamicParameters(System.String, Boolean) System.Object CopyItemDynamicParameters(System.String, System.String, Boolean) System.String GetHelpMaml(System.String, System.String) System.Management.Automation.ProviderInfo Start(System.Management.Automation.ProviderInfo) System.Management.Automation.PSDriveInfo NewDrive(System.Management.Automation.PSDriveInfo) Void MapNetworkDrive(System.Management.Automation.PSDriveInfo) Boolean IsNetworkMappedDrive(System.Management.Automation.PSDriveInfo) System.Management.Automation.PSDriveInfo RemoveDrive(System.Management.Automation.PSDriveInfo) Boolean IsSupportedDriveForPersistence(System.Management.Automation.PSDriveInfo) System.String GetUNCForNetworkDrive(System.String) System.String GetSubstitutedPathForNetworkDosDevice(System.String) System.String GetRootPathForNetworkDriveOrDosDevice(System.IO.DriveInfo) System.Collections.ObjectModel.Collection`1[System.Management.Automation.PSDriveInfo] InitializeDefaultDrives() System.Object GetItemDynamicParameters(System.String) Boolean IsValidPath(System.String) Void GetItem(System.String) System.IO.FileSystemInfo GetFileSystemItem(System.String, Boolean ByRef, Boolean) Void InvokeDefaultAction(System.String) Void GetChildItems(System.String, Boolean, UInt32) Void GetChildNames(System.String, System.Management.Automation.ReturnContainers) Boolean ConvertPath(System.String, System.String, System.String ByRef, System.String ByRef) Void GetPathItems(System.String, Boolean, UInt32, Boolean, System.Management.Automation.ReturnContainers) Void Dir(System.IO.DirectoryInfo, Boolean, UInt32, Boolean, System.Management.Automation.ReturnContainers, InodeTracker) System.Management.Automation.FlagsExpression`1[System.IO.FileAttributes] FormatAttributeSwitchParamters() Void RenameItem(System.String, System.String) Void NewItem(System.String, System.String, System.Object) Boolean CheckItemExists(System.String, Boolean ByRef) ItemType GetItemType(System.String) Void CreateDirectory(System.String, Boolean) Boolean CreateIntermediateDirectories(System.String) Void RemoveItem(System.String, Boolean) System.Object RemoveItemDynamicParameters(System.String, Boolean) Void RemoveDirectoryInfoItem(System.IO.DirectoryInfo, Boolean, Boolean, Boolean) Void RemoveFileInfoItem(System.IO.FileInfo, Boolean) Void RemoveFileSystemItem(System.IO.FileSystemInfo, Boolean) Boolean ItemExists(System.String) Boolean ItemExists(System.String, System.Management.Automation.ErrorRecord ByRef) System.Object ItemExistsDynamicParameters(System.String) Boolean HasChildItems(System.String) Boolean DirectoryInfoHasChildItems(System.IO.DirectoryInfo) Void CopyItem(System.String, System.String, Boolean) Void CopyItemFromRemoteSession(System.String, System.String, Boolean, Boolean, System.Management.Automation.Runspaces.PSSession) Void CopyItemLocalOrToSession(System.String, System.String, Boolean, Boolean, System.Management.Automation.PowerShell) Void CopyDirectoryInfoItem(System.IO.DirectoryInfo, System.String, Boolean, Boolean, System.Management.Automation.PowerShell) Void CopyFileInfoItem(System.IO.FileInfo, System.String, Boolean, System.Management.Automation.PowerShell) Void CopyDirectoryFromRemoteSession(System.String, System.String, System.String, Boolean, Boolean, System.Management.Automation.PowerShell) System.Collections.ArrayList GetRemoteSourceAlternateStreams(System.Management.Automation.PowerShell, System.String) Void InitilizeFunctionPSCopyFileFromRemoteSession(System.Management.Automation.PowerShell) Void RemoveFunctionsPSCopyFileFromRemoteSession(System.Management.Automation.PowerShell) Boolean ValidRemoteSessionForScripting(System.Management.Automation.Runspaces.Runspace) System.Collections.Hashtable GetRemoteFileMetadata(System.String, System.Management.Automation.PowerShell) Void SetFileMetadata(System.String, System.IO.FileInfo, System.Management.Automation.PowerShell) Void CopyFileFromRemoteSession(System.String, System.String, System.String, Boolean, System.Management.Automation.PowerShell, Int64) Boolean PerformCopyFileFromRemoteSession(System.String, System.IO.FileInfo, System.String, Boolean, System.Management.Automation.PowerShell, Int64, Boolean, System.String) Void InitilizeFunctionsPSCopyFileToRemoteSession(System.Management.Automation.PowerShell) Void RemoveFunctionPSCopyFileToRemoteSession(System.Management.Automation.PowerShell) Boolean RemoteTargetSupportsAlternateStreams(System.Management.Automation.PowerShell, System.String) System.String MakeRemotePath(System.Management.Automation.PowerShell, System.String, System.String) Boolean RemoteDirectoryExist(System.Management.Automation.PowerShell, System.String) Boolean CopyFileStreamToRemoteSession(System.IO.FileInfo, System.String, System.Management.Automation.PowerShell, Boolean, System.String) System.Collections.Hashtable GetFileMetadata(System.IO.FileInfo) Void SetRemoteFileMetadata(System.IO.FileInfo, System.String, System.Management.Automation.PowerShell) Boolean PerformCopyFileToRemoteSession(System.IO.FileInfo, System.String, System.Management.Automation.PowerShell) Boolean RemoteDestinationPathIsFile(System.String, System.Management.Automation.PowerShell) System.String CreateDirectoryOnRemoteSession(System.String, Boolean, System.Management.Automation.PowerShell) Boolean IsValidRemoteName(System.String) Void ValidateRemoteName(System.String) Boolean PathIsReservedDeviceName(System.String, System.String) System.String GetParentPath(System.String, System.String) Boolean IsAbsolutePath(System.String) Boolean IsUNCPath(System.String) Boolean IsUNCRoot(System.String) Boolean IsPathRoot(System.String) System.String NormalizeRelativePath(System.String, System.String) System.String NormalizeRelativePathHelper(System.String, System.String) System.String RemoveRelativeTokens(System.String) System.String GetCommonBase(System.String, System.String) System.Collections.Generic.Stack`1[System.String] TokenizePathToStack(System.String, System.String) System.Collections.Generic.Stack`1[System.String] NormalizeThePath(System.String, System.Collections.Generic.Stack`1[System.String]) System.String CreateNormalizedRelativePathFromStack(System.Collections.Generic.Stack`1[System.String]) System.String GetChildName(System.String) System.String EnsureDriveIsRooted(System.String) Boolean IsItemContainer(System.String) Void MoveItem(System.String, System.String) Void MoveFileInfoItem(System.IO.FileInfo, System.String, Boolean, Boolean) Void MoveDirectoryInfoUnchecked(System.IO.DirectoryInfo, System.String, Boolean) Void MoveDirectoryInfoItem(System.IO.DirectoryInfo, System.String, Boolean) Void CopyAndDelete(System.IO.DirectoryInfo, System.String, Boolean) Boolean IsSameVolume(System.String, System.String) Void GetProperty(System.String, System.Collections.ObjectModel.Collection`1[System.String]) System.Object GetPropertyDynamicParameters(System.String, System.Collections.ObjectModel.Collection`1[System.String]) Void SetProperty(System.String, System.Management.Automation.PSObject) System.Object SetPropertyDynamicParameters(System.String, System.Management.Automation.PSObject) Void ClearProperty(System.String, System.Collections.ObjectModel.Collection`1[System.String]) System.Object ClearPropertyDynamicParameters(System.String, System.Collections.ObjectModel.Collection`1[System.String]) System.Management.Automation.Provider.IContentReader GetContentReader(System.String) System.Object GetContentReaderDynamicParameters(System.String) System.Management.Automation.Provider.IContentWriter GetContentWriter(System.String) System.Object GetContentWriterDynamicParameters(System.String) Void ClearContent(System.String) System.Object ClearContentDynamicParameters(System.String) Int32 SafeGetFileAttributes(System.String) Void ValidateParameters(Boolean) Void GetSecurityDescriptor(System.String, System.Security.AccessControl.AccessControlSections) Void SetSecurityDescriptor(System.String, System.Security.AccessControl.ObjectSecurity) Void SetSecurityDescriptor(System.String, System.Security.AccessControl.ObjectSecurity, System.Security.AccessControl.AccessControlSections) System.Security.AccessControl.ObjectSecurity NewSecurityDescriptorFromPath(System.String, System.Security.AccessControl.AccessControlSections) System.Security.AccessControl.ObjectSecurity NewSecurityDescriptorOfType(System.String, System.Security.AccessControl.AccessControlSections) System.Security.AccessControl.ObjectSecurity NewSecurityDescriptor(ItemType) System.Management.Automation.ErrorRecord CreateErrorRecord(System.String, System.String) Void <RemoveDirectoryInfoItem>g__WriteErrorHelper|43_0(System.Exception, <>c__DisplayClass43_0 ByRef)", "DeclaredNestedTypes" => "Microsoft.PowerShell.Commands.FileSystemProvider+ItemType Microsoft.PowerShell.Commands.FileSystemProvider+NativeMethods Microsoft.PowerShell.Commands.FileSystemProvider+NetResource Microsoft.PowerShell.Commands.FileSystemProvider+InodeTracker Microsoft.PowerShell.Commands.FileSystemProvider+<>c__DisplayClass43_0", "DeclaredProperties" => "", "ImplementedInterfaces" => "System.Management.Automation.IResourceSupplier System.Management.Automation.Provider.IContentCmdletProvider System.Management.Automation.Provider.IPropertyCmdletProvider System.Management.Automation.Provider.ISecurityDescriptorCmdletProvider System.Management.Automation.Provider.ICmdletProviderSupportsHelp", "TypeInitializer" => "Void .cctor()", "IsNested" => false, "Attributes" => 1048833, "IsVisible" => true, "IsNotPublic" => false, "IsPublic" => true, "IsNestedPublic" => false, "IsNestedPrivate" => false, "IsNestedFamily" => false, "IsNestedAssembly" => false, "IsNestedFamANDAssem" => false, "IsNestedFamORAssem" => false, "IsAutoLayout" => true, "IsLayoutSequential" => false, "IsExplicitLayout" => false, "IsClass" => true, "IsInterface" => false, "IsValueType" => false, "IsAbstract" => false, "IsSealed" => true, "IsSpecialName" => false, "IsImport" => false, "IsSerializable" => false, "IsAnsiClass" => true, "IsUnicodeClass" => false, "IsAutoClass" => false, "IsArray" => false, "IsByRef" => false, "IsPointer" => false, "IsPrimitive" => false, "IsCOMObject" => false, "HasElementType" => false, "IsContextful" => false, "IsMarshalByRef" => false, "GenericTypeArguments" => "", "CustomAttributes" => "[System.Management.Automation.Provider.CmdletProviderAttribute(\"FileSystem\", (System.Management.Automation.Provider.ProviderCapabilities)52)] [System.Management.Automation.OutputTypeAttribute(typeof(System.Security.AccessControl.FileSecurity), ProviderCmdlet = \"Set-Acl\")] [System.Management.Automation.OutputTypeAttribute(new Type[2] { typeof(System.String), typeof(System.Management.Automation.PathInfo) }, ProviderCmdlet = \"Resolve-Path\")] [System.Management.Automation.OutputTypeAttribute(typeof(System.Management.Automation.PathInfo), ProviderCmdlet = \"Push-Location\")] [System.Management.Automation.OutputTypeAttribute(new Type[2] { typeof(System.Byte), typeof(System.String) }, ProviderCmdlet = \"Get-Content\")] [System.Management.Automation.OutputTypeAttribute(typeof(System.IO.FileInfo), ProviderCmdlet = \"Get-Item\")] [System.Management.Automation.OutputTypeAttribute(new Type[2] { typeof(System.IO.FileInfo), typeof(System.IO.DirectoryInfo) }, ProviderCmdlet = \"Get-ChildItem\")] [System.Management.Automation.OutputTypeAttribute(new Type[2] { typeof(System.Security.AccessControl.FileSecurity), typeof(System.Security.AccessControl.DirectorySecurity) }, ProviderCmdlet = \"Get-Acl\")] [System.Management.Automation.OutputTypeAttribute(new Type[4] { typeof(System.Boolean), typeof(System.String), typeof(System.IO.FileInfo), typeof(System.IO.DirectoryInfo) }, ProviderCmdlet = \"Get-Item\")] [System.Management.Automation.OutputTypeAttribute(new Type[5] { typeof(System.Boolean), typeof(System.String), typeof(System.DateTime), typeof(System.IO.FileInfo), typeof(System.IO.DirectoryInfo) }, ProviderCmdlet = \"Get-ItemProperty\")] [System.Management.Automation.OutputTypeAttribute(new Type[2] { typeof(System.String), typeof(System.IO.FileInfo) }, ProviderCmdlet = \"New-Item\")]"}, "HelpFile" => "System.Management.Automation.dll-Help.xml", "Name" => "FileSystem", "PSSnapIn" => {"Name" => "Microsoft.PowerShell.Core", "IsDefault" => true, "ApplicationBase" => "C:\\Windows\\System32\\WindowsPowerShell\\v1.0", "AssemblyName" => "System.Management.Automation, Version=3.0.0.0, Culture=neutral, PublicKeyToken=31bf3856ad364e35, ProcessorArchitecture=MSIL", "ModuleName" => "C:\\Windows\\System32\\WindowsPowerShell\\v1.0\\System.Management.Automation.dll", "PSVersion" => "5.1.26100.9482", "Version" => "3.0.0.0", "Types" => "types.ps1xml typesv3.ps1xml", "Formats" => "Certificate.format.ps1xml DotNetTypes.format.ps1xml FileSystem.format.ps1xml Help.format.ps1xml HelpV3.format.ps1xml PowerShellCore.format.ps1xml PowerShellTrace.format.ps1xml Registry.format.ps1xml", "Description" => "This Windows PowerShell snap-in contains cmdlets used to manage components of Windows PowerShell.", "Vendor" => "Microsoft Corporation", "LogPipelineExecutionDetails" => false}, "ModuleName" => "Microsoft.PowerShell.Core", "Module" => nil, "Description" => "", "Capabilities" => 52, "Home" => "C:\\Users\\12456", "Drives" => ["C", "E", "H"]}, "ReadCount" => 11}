# 技术方案设计文档
## 项目：AudioSwitch（音频设备快速切换工具）

---

## 1. 软件类型

**Windows 桌面软件（便携版，单个 exe，免安装）**

- 目标系统：Windows 11（兼容 Windows 10）。
- 运行形态：双击 exe 直接运行，收在系统托盘，不占屏幕。

---

## 2. 核心技术难点（先说清楚命门在哪）

要"切换 Windows 默认麦克风/音箱"，代码必须调用 Windows 的一个底层接口：
**IPolicyConfig（COM 接口）**。

- 它是 Windows 内部用来设置"默认音频设备"的接口，虽然微软没有公开文档，但**非常稳定**，市面上所有同类软件（SoundSwitch、EarTrumpet）都用它。
- 好消息：这个难题**有现成的开源答案**（SoundSwitch 就是 C# 写的，代码可直接参考），我们不用从零硬啃。

---

## 3. 候选技术方案对比

| 方案 | 语言/框架 | 体积 | UI 能否做到 Win11 高级感 | 对新手难度 |
|---|---|---|---|---|
| **A. C# + WPF**（.NET 8） | C# | 小/中 | ✅ 能（需美化） | 低（有 SoundSwitch 参考） |
| **B. C# + WinUI 3** | C# | 中/大 | ✅✅ 原生 Win11，最美 | 高（坑多） |
| **C. Tauri**（Rust + 网页前端） | Rust | ✅✅ 极小（几 MB） | ✅ 很强 | 极高（Rust 难） |
| **D. Electron**（网页技术） | JS | ❌ 巨大（100MB+） | ✅ 强 | 中 |
| **E. Python + PyQt** | Python | ❌ 大（几十 MB） | ⚠️ 费劲 | 中 |
| **F. AutoHotkey** | 脚本 | ✅ 极小 | ❌ 简陋 | 低 |

---

## 4. 推荐方案：A. C# + WPF（.NET 8）

### 为什么选它（三条理由）

1. **有"参考答案"**：SoundSwitch 就是用 C# + WPF 写的，最难的"切换设备"代码可以直接参考它的开源实现，这是最大的优势。
2. **UI 能做得高级**：WPF 可以做圆角、亚克力毛玻璃、流畅动画，虽然要花点功夫美化，但完全能达到"Win11 高级感"。
3. **最适合小白 + AI 协作**：C# 生态成熟、教程最多、报错好排查，我帮你维护也最顺手。

### 各部分用什么（技术选型明细）

| 功能 | 用什么 |
|---|---|
| 切换默认麦克风/音箱 | IPolicyConfig（参考 SoundSwitch） |
| 列出所有音频设备 | Windows 设备枚举 API |
| 界面 | WPF（XAML）+ Win11 风格美化 |
| 系统托盘 | 托盘图标组件 |
| 全局快捷键 | Windows 快捷键 API（RegisterHotKey） |
| 开机自启动 | 写注册表（Run 键） |
| 打包 | .NET 单文件发布（single-file publish） |

### 优点
- 成熟稳定，参考资料最多；
- 我能帮你高效维护和排查问题；
- 未来扩展方便（加功能、加设置都很顺）。

### 缺点（诚实说明）
- WPF 默认界面不"高级"，需要额外美化；
- **体积取舍**：.NET 有两种打包方式，各有代价（见第 6 节，需要你拍板）。

### 未来能否升级
- ✅ 完全可以。加"按软件路由声音""独立音量""热插拔自动切换"等扩展功能，WPF + C# 都能平滑支持。

---

## 5. 为什么排除其他方案（一句话版）

- **WinUI 3**：UI 最美，但打包和调试坑多，对新手不友好，且体积并不小。
- **Tauri**：体积最小，但 Rust 太难，切换音频设备的 Rust 实现复杂度极高，不适合小白。
- **Electron**：体积巨大（100MB+），直接违背你"小巧"的核心要求。
- **Python**：打包后体积大，UI 做 Win11 风格费劲。
- **AutoHotkey**：UI 简陋，做不出你要的高级界面。

---

## 6. 需要你拍板的取舍：体积 vs 免安装

.NET 打包有两种模式，各有取舍：

| 模式 | 体积 | 是否免安装 | 说明 |
|---|---|---|---|
| **框架依赖** | exe 很小（约 1-2 MB） | ❌ 需电脑装 .NET 8 运行时 | 第一次用要装一次 .NET（约 60MB，装一次以后所有 .NET 软件共用） |
| **自包含单文件** | exe 较大（约 60-150 MB） | ✅ 完全免安装 | 把运行环境一起打进 exe，双击就能跑，但体积上去了 |

**现实情况**：
- "极小体积（几 MB）"和"真正免安装、双击就跑"这两个目标，用 C# 很难同时满足——这是 .NET 的客观限制。
- 追求极致小体积的 Tauri（几 MB + 免安装）能做到，但 Rust 对小白不友好。

---

## 7. 已确认的决定（2026-08 拍板）

- [x] **体积取舍：选择 A（框架依赖）**——exe 保持 1-2MB，用户首次使用需安装 .NET 8 运行时。
- [x] **界面语言：先只做中文**。

> 打包方式：.NET 框架依赖发布（framework-dependent），而非自包含。
> 这意味着：开发阶段先保证功能跑通；最终交付时附带一份《.NET 8 运行时安装指引》给首次使用者。
